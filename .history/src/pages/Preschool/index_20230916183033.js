import React from 'react';

const index = () => {
  return <Container>
    <InnerContainer>

    </InnerContainer>
    <InnerContainer>
        </>
    </InnerContainer>
  </Container>;
};

export default index;
